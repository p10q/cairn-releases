# Cairn compatibility files

`CHANGELOG.md` is the stable release history. `appcast.xml` remains only as a
compatibility bridge for Cairn versions released before 2.5.0.

Current app builds use the unlisted Sparkle channel configured in the private
Cairn source repository. Do not place DMGs, deltas, a latest-DMG alias, or
direct artifact links in this directory.
